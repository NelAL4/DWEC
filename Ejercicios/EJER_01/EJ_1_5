//"declarar/formatear" un objeto sin crearlo (crando una clase):

const alumnos =[
    {nombre:"Pepe", apellidos:"Romeo Santos", calificacion:7, aprobado:true},
    {nombre:"Juan",apellidos:"Pérez Soriano", calificacion:5, aprobado:true},
    {nombre:"mikel", apellidos:"Oyarzabal Ugarte", calificacion:3, aprobado:false} 
]
//.map pasa 3 parametros siempre, el primero obligatorio los otros dos opcionales(los pides asignandoles un nombre)
//    1º parametro(alumno): elemento actual del array que recorre
//    2º parametro(id): úumero de posicion del elemento en que se encuentra (i del bucle for)
//    3º parametro: array original completo 
const alumnosId = alumnos.map(function(alumno,id){ 
    alumno.id=id++;
})

const aprobados = alumnos.filter(alumno => alumno.calificacion>=5)
// const aprobados = alumnos.filter(function(alumno) {
//   return alumno.calificacion >= 5; 
//   }
// );

aprobados.forEach(aprobado=>console.log(`!Felicidades ${aprobado.nombre}, has aprobado con ${aprobado.calificacion}¡`))
// aprobados.forEach(function(aprobado) {
//   console.log("¡Felicidades " + aprobado.nombre + ", has aprobado con " + aprobado.calificacion + "!");
// });

alumnos.forEach(function(alumno){
    if((alumno.calificacion<5 && alumno.aprobado) || (alumno.calificacion>4 && !alumno.aprobado) ){
        console.log(`Incoherencia en el registro de ${alumno.nombre}: calificacion = ${alumno.calificacion}, aprobado = ${alumno.aprobado}`)
    }
})
