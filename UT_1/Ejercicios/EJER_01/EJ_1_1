const nombre="Nel"
let edad=21
const tieneMascota=true

edad=22
//tieneMascota=false //causa errpr porque const no puede jamás cambiar su valor
console.log(nombre,"=",typeof nombre,
            edad, "=", typeof edad,
            tieneMascota, "=", typeof tieneMascota)

//template Strings:
console.log(`${nombre} tiene ${edad} años y ${tieneMascota? "tiene mascota":"no tiene mascota"}`)
